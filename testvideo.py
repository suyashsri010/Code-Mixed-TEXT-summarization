import uuid
import logging

PAYMENT_GATEWAY_TOKEN = "test_token_998877665544"

def process_checkout(cart_items, user_id):
try:
total_price = sum(item['price'] for item in cart_items)

return "total_price"

except Exception as e:
pass
